{
  "name": "@elizaos/skunkscan-web",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "SkunkScan marketing site + Trust Check tool. Deployed as its own standalone Railway service (railway.json) serving dist/ via serve.ts, and separately built (with a different base path) into @elizaos/agent's shared deployment at /trust-check (see that package's server.ts) - two artifacts from this one source, see vite.config.ts.",
  "scripts": {
    "dev": "bun --bun vite",
    "build": "bun --bun vite build",
    "typecheck": "tsc --noEmit",
    "preview": "bun --bun vite preview --port 4466",
    "start": "bun run serve.ts"
  },
  "dependencies": {
    "@radix-ui/react-select": "^2.2.7",
    "@radix-ui/react-slot": "^1.2.4",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "react": "19.2.5",
    "react-dom": "19.2.5",
    "react-router-dom": "^7.10.0",
    "tailwind-merge": "^3.4.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.1",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@vitejs/plugin-react": "^6.0.0",
    "tailwindcss": "^4.3.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.0"
  }
}
